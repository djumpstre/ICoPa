"""
Input is the inventory in dictionary format form the Django database.

This export
 - can convert it to the YAML format, return a YAML string
 - create a graph from the inventory data, and return a graph image for visualization

"""
